
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Default deny all reads and writes to Cloud Storage
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
