rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Lock down all client-side access by default.
    // Server-side access via the Admin SDK is not affected by these rules.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
